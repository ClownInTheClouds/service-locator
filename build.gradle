plugins {
    id 'publish-plugin' version '1.0.0' apply false
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'publish-plugin'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(25)
        }
    }
}